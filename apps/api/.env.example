# HTTP server
PORT=3000

# PostgreSQL (connection is intentionally not created by the foundation yet)
DATABASE_URL=postgresql://jcp:change-me@localhost:5432/jain_community_platform

# Redis (connection is intentionally not created by the foundation yet)
REDIS_URL=redis://localhost:6379

# Temporary bootstrap tenant resolver configuration.
# The persistent tenant/domain resolver will move to PostgreSQL in the tenant foundation.
TENANT_HOSTNAME=badebabakharadi.com
TENANT_ID=bade-baba-kharadi
TENANT_NAME=Bade Baba Kharadi

# Authentication provider (OIDC/JWT compatible; configure before enabling protected routes)
AUTH_ISSUER=
AUTH_AUDIENCE=
AUTH_JWKS_URL=
